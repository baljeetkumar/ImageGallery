<% include ../partials/header %>

<div class="container">
  <header class="jumbotron">
   <div class="container">
        <h1>Home</h1>
        <p>Welcome To The World Of Social Media</p>
        <p> <a class ="btn btn-primary btn-large" href="/campgrounds/new">Add New Post</a> </p>
   </div>
  
  
  </header>
  
  <div class="row text-center" style="display:flex; flex-wrap:wrap;">
   
     <% campg.forEach(function(campp){ %>
     
     <div class="col-md-4 col-sm-6">
         <div class="thumbnail">
           <img src=" <%=campp.image %>">
          
           
           <div class="caption">
                    <h4> <%=campp.name %> </h4>
           </div>
           <a href="/campgrounds/<%= campp._id %>" class="btn btn-info">More</a>
           
         </div>
     </div>
     
 <% }); %>
 
  </div>

 
 </div>
 <% include ../partials/footer %>
 